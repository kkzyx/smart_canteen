<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
				"http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.holly.mapper.PickupAddressMapper">
	
	<!-- 动态条件查询地址簿列表 -->
	<select id="list" parameterType="com.holly.entity.AddressBook" resultType="com.holly.entity.AddressBook">
		select * from `address_book`
		<where>
			<if test="userId != null">
				and `user_id` = #{userId}
			</if>
			<if test="phone != null">
				and `phone` = #{phone}
			</if>
			<if test="isDefault != null">
				and `is_default` = #{isDefault}
			</if>
		</where>
	</select>
	
	<!-- 更新地址簿 -->
	<update id="update" parameterType="com.holly.entity.AddressBook">
		update `address_book`
		<set>
			<if test="consignee != null">
				`consignee` = #{consignee},
			</if>
			<if test="sex != null">
				`sex` = #{sex},
			</if>
			<if test="phone != null">
				`phone` = #{phone},
			</if>
			<if test="detail != null">
				`detail` = #{detail},
			</if>
			<if test="label != null">
				`label` = #{label},
			</if>
			<if test="isDefault != null">
				`is_default` = #{isDefault},
			</if>
		</set>
		where `id` = #{id}
	</update>
</mapper>