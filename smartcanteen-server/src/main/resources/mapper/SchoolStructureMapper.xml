<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.holly.mapper.SchoolStructureMapper">

    <resultMap id="BaseResultMap" type="com.holly.entity.SchoolStructure">
        <id property="id" column="id" />
        <result property="parentId" column="parent_id" />
        <result property="name" column="name" />
        <result property="code" column="code" />
        <result property="type" column="type" />
        <result property="sort" column="sort" />
        <result property="createTime" column="create_time" />
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT id, parent_id, name, code, type, sort, create_time
        FROM school_structure
        ORDER BY sort ASC, create_time DESC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM school_structure WHERE id = #{id}
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO school_structure (parent_id, name, code, type, sort)
        VALUES (#{parentId}, #{name}, #{code}, #{type}, #{sort})
    </insert>

    <update id="update">
        UPDATE school_structure
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="type != null">type = #{type},</if>
            <if test="sort != null">sort = #{sort},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM school_structure WHERE id = #{id}
    </delete>

</mapper>